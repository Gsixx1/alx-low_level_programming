#include "main.h"

/**
 * main - this tests the function that prints if
 * integer is positive or negative.
 *
 * Description: This project only needs to be debugged, and
 * the output will print the code.
 *
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);

	return (0);
}
