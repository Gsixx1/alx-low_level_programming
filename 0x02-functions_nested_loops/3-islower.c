#include "main.h"
/**
 * _islower - check the code.
 *@c: int parameter
 * Description: This program writes a function that
 * checks for lowercase.
 *
 * Return: 0
 *
 */
int _islower(int c)
{
	if (c == 'c')
	{
		return (1);
	}
	else
		return (0);
}
