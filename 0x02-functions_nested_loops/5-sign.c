#include "main.h"

/**
 * print_sign - check the code.
 * @n: function parameter or the argument of the function.
 *
 * Return: 0
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+')
			return (1);
	}
	else if (n < 0)
	{
		_putchar('-')
	}
	else
	{
		_putchar('0);
		return (0);
	}
}
