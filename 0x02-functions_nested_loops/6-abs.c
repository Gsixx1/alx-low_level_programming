#include "main.h"
#include <stdio.h>

/**
 * _abs - this is the name of the function
 * it checks the code.
 *
 * @c: thisi is the int parameter that will
 * be used for the argument of the function.
 *
 * Return: 0
 */
int _abs(int c)
{
	if (c > 0 || C == 0)
	{
		return (c);
	}
	else
		return (c * -1);
}
