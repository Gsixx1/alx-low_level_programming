#include <stdio.h>

/**
 * main - Used to print the string in the printf function
 *
 * Description: the main function is used to print
 * with proper grammar, but the outcome is a piece of art, followed
 * by a new line
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
